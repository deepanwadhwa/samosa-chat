#!/bin/sh
# Installer control-flow fixture only. Real OCR has separate runtime tests.
case "$1" in
  --check) echo '{"ok":true,"engine":"tesseract"}' ;;
  --version) echo 'samosa-ocr installer-fixture (tesseract)' ;;
  *) exit 64 ;;
esac
