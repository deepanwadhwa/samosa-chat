# Stabilization work log — 2026-07-14

This log records actions taken after approval to execute the documented next
steps. It complements the diagnosis and regression ledger; it is not a release
announcement.

## Baseline freeze

- Recorded the group-32 artifact provenance, sizes, format, manifest hash,
  resident hash, and configuration hashes in `GROUP32_BASELINE.md`.
- Did not duplicate or monolithically rehash the 20.94 GB expert store. The
  manifest already contains and validated every per-expert digest.

## Fail-fast regression gate

- Added `tools/run_regression_gate.py` with bounded token budgets, two-core
  defaults, structured stdout/stderr/results, natural-versus-forced closure,
  correctness markers, repetition checks, model-stop checks, and pre/post
  machine telemetry.
- Safety polling stops a run for thermal/performance warnings, throttled pages,
  low memory/disk margin, or more than 64 MB of new swapouts by default.
- Added unit coverage for stats parsing, forced-close rejection, direct-mode
  correctness, and swap-growth termination.
- Encoded the already-observed arithmetic variant-A failure rather than
  repeating a 114.94 GB-read run solely for the new harness.
- Per the predeclared fail-fast rule, variant B was not run after variant A
  failed. See `REGRESSION_LEDGER.md`.

## Mixed-precision prototype

- Added an experimental manifest format for grouped-q4 gate/up with row-q8
  down projections: `groupwise-q4-gate-up-row-q8-down-v1`.
- Added converter encoding, resume-signature/reuse validation, runtime manifest
  validation, expert views, and format logging. MTP experts remain all-q8.
- Reused the existing grouped-q4 and row-q8 float/int8-activation kernels; no
  new unverified arithmetic kernel was introduced.
- Added Python layout/dequantization tests and C float/integer-dot q8-down
  tests. The full test suite and OpenMP engine build pass.
- A regular mixed expert is 2,375,680 bytes versus 1,966,080 bytes for the
  group-32 all-q4 baseline (+20.83%). A full mixed expert store is estimated at
  25,136,463,872 bytes; with the existing resident file, core model storage is
  approximately 28,151,520,064 bytes.
- No full mixed conversion was started. It cannot coexist with the current
  artifact while retaining the 15 GB free-disk safety floor.
- Rebuilt the OpenMP engine and repeated the one-token group-32 loader smoke to
  verify backward compatibility with manifests that predate the optional
  `down_bits` field. It returned `OK`, reported `group=32 down_bits=4`, peaked
  at 3.20 GB RSS, and read 4.05 GB of experts without pressure events.

## External disk observation

- Free disk unexpectedly fell from 38 to 32 GiB.
- Cause: parent-repository Git temporary pack
  `.git/objects/pack/tmp_pack_sbzINX`, approximately 6.3 GiB, created at 10:06.
- `lsof` found no open handle and no `git`, `pack-objects`, or `index-pack`
  process was running when checked.
- The file was not deleted because it was not included in the earlier explicit
  deletion authorization. The older preserved `tmp_pack_N3tIw6` is 324 MiB.

## Deterministic route/cache A/B

- Added and tested `tools/analyze_route_trace.py`. It reads route-v2 effective
  expert selections, reports current-layout adjacency/span, and produces a
  greedy co-activation candidate without changing the model.
- Captured a bounded 1,400-record trace for a 21-token direct prompt followed
  by 15 generated tokens. Raw trace and analysis are stored outside the Git
  worktree in `../c/samosa-route-cache-ab.jsonl` and
  `../c/samosa-route-cache-analysis.json`.
- Default 2.07 GB cache: 3.75 GB peak RSS, 10.00 GB expert reads, 3.76 GB
  avoided, 6.37 tok/s decode, 5.416 seconds total.
- Exact route replay with a 3.00 GB cache: 4.85 GB peak RSS, 7.46 GB expert
  reads, 6.30 GB avoided, 6.21 tok/s decode, 5.583 seconds total. Replay
  verified all 1,400 records and output remained `1, 2, 3, 4, 5.`
- The larger cache cut requested expert bytes by 25.4% but increased peak RSS
  by 1.10 GB and slowed this short run by 3.1%. It is not promoted to default.
- On this short trace, numeric layout averaged 0.219 adjacent selected pairs
  per routing record; the greedy trace-specific candidate averaged 4.265.
  This is evidence that adjacency can be manufactured, not that it generalizes
  or reduces cache misses. No artifact reorder or coalesced-read implementation
  is justified until diverse traces and an actual I/O A/B exist.
- Post-run safety: swapouts unchanged, 78% memory free, zero throttled pages,
  no macOS thermal/performance warning, 32 GiB disk free.

## Atomic installer and format-aware doctor

- Replaced live-path downloads with immutable versioned release directories.
- Added `release-manifest.tsv` packaging with SHA-256, exact byte size, and
  relative path for every installed payload.
- The installer validates manifest paths, computes remaining side-by-side disk
  need before download, resumes only inside an inactive release-specific
  directory, verifies every file, compiles the staged engine, and smoke-tests
  it before activation.
- Activation is one atomic `current` symlink replacement. The stable launcher
  resolves the current release; model, engine, tokenizer, and wrapper therefore
  switch together.
- Existing/previous releases are not silently deleted. `samosa doctor` reports
  retained rollback releases and a pre-versioned legacy model directory.
- `samosa doctor` now identifies legacy whole-row q4, groupwise q4 with its
  group size, and the experimental mixed group-q4/q8-down format.
- Fixed the stale packaging script paths/names and included
  `repetition_guard.h`, which the engine now requires.
- Added an integration test covering fresh legacy install, atomic upgrade to a
  groupwise release, and a corrupted remote expert payload. The corrupt install
  failed in inactive staging and left the active release pointer unchanged.

## Protected non-actions

- The expanded five-variant/three-seed matrix was **not** run because its
  prerequisite group-32 fail-fast cell failed. Running it would contradict the
  declared gate and create unnecessary multi-terabyte expert traffic.
- No Hugging Face package was assembled or uploaded.
- The README generation showcase was not replaced, and no group-32 quality or
  speed claim was promoted.
- A full mixed artifact was not converted because current free space cannot
  hold it beside the frozen baseline while retaining the safety floor.

## Final verification and machine state

- Removed only generated local test binaries and Python bytecode caches; no
  model artifact, user asset, source change, or preserved Git object was
  deleted.
- `make test` passes, including converter layout/dequantization, grouped-q4 and
  q8-down kernel coverage, regression-gate parsing/safety logic, route analysis,
  wrapper behavior, repetition guard, and the atomic installer failure test.
- `make omp`, Python syntax compilation, shell syntax validation for the
  installer, and `git diff --check` pass.
- Final machine check: 80% memory free, zero throttled pages, and no recorded
  macOS thermal or performance warning. Swapout count remained at 498,913; it
  did not increase during the bounded route/cache comparison.
- Final free space is approximately 31 GiB. Retaining the frozen group-32
  baseline, the estimated 28.15 GB mixed model, and the 15 GB safety floor
  requires at least approximately 44 GiB free before starting conversion.
- No commit, push, package publication, model upload, or default-cache change
  was performed.

## Upstream-control correction

- Reopened the quantization conclusion after identifying that the failed
  arithmetic cell combined a 256-token thinking cap with a 512-token overall
  cap and had no upstream arm.
- Verified against Qwen's official documentation that budget exhaustion should
  append a natural-language early-stop transition before `</think>`. Samosa
  previously injected only the raw close token.
- Added `tools/run_openrouter_control.py`. It reads the key from `.env` without
  printing it, passes authorization to curl through stdin rather than argv,
  pins one provider with fallback disabled, preserves structured reasoning and
  final output, and reports token/cost/closure/correctness distributions.
- Added `.env` to `.gitignore` after discovering that the key file was
  untracked but not ignored.
- OpenRouter currently exposes the exact Qwen3.6 model through FP8 providers,
  not BF16. The control is explicitly labeled behavioral/upstream-compatible,
  not an unquantized numerical oracle.
- Ran variants A/B at seeds 11, 29, and 47 for $0.00412958 total. All six
  closed naturally and answered correctly. Reasoning used 353--616 tokens;
  total completion used 587--928 tokens. Every run exceeded the old thinking
  cap, and three exceeded the old outer cap.
- Corrected a second scoring defect: exact `7 red`/`4 blue` substrings rejected
  correct answers formatted as `Red balls: 7`. Both local and upstream runners
  now support task-specific regex assertions.
- A repeated hosted seed-11 request produced a different trajectory, so hosted
  seeds are treated as distributional samples rather than token-identical
  controls across runtimes.

## Correct Qwen budget transition

- Added `src/thinking_budget.h` with the exact published Qwen early-stop text
  and a bounded transition scheduler.
- The runtime tokenizes the complete transition, verifies that it contains
  `</think>`, injects the whole sequence only after budget exhaustion, and
  continues its trailing tokens even after the close marker.
- Natural closure bypasses injection. The transition remains containment, not
  an automatic quality pass.
- Added focused C coverage and included the new header in packaging, the
  release manifest, staged compilation, and atomic installer test.
- The complete test suite, OpenMP build, Python/shell syntax checks, staged
  fresh install, corrupt-upgrade rollback, and `git diff --check` pass.

## Recalibrated local group-32 arm

- Ran exactly one two-core local cell: arithmetic variant A, seed 11, 1,024
  thinking budget, and 2,048 outer ceiling. The outer cap exceeded all upstream
  pilot completions while bounding a pathological local trajectory.
- Result: pass. The model closed thinking naturally, returned the correct
  7-red/4-blue/11-total answer with a valid check, and emitted end-of-turn after
  933 generated tokens. The forced transition was not used.
- Timing: 5.842 s prefill, 192.161 s decode, 198.003 s total, 4.85 tok/s.
- I/O/RSS: 376.77 GB expert bytes read, 217.28 GB avoided, 3.23 GB peak RSS.
- Safety: swapouts stayed at 498,913, throttled pages stayed zero, no cache
  pressure event or macOS thermal/performance warning occurred, and post-run
  memory free was 59% in the structured sample (62% at the following check).
- The original 256/512 failure is now recorded as an invalid/confounded gate,
  not evidence that group-32 caused instability. Broad release stability is
  still unproven and now requires upstream-calibrated parity by task family.
- Stopped local generation after this decisive cell to avoid another roughly
  377 GB read pass without a predeclared need.

## Revised artifact decision

- Paused the full mixed q4/q8-down conversion. The control no longer justifies
  assuming that a larger artifact is the next fix.
- Documented activation-aware equivalent scaling as a same-size q4 candidate
  if a real local/upstream gap remains. The current teacher stream records
  logits but not per-expert down-input channel statistics, so a valid AWQ-style
  prototype first needs a representative BF16 activation-statistics hook.
- Free space fell again because a second unowned Git temporary pack,
  `.git/objects/pack/tmp_pack_ezUG1M`, appeared at approximately 4.6 GiB. No
  Git pack process or open handle was found. It was not deleted.
- No model conversion, upload, publication, commit, push, README showcase
  replacement, or default thinking-budget increase was performed.

## Resident server and app launcher

- Refactored generation ownership so a resident process can reuse the loaded
  model, expert cache, and tokenizer while reclaiming per-request KV/DeltaNet
  state, sampler bitmaps, and teacher state. Added cooperative cancellation
  checks before and between generated tokens.
- Added a dependency-free local HTTP layer bound to `127.0.0.1`, with capped
  headers/bodies, a bounded FIFO scheduler, `429` queue-full and `503` shutdown
  behavior, and clean listener termination.
- Implemented `/healthz`, `/v1/models`, `/v1/chat/completions`, `/v1/cancel`,
  and `/v1/shutdown`. SSE keeps reasoning separate from final content and
  reports natural, budget-transition, repetition, or cancelled closure.
- Added sanitized `conversation_id` persistence under `~/.samosa/chats/` using
  sealed atomic session snapshots. Snapshot-save success is exposed in terminal
  response telemetry. This is disk-backed restore, not the planned four-slot
  in-RAM LRU, and the distinction is recorded in `APP_TASKS.md` and
  `SERVE_API.md`.
- Added `samosa serve`, `samosa app`, and `samosa serve --stop`; the background
  launcher maintains one PID and opens the loopback URL. Packaging now carries
  the HTTP header and links pthreads.
- Added component tests for JSON escaping, bounded admission, 20 sequential
  socket connections, health/models/root/cancel/404/shutdown, clean accept-loop
  exit, and wrapper launch/stop behavior.

## Bounded real server acceptance

- Foreground SSE smoke returned `OK` after one completion token, reported
  3.29 GB RSS and 4.05 GB expert reads, and shut down cleanly.
- The real launcher kept one PID across two invocations. Two turns in one
  conversation returned `OK` then `YES`; the second restored a 64 MB sealed
  session instead of re-prefilling history, and the launcher stopped cleanly.
- With the wait queue set to zero, a concurrent second request received
  `429 queue_full`. `/v1/cancel` stopped the active long request before any
  completion token, emitted terminal `finish_reason=cancelled`, read 4.94 GB
  of experts, peaked at 3.32 GB RSS, and shut down cleanly.
- Acceptance used temporary state only and removed it afterwards. Swapouts
  remained at 498,913, throttled pages remained zero, and the final structured
  memory sample reported 66% free. No model artifact was converted or written.
- These checks establish the resident-server foundation, not consumer-app
  readiness. The Phase A1 browser UI, in-RAM slot pool/write batching, bounded
  long-context regression, and declared soak/package gates remain open.
- After adding server/session telemetry, the complete `make test` suite, the
  OpenMP engine build, shell syntax checks, Python compilation, installer
  fresh/upgrade/corrupt rollback, and `git diff --check` all passed.

## README identity correction

- The first mascot files were PNG containers but not transparent artwork: the
  grey/white checkerboard had been baked into their pixels and therefore showed
  visibly on GitHub's dark theme.
- Redrew the mascot as a cleaner brand illustration using the original only as
  a character reference, generated it against a flat chroma background, then
  removed that background locally with a soft matte, despill, and one-pixel
  edge contraction.
- Replaced both project assets with optimized true-alpha RGBA PNGs at 400×400
  and 800×800. Validation confirmed transparent corners, a full 0–255 alpha
  range, complete subject bounds, and clean presentation on both `#0d1117` and
  white backgrounds.
- Reworked the README opening into a centered product header with a concise
  local-first tagline and the transparent 800 px mascot source rendered at
  210 px.

## README release-truth audit

- Compared the GitHub source tree with the live Hugging Face repository rather
  than treating them as one release. The published package still contains the
  legacy whole-row q4 artifact and older CLI wrapper; it has no
  `release-manifest.tsv`, server header, resident-server wrapper commands, or
  group-32 artifact.
- Rewrote the README so install instructions describe the package users can
  actually download today, while group-32, revised thinking controls, the
  localhost server, launcher, and atomic installer are labeled as source/local
  previews until the next verified artifact upload.
- Moved colibrì/JustVugg and Qwen-team attribution to the opening section and
  enumerated the Qwen engine, quantization, session, server, installer, cache,
  safety, and regression work added in this repository.
- Replaced the ambiguous “no GPU” claim with the precise statement: the current
  macOS build is CPU-only, uses NEON/SDOT plus optional OpenMP, does not use
  Metal, and therefore requires no dedicated GPU.
- Removed the obsolete full-page HTML source dump while retaining its unedited
  rendered screenshot, prompt, and measured run statistics. Corrected the
  example command to include the published wrapper's `--long` ceiling.

## Storage cleanup and workspace migration

- Removed uv's re-downloadable package cache, reclaiming roughly 12–13 GiB.
- Verified the old `samosa_upload` bundle against its complete checksum ledger
  before removal.
- Moved the product repository to `/Users/deepanwadhwa/Documents/samosa-chat`.
- Moved the unique group-32 model and tokenizer to
  `/Users/deepanwadhwa/Documents/samosa-models` after recording SHA-256 hashes.
- Preserved 1.1 GiB of experiments and benchmark evidence under
  `/Users/deepanwadhwa/Documents/samosa-lab`.
- Removed the re-downloadable legacy model, resident-int8 experiment, old
  virtual environment, upload staging tree, and 61 GiB outer Git database.
- Verified the moved repository and preserved model sizes. Free disk space
  increased from approximately 13 GiB to 95 GiB.
- Full details: [STORAGE_MIGRATION_2026-07-14.md](STORAGE_MIGRATION_2026-07-14.md).

## Interactive local app

- Added a 32 KB single-file, dependency-free browser UI served by the resident
  C process, plus a fixed local route for the transparent PNG mascot.
- Added SSE answer/reasoning streaming, stop/cancel, responsive conversation
  navigation, browser-local history, safe lightweight markdown rendering,
  direct/general/code controls, seed and output ceilings, and live speed/RSS/
  closure telemetry. The UI makes no external request.
- Added the app and logo to the atomic installer, package manifest, wrapper
  environment, component tests, and Hugging Face packaging tool.
- The first real app-path request exposed that the server sink did not stop on
  Qwen's `<|im_end|>`/`<|endoftext|>` token IDs and streamed control tokens.
  Added explicit stop IDs and focused regression coverage.
- The corrected real group-32 run streamed exactly `Samosa app works locally.`,
  stopped naturally after 8 generated tokens, saved a 67.2 MB session, decoded
  at 5.13 tok/s on two threads, peaked at 3.28 GB RSS, and read 8.76 GB from the
  expert store.
- Added a reproducible local development installer that hard-links the model
  instead of duplicating 24 GB. The resulting `samosa doctor` check passed.
- Fixed streaming scroll lock after live use showed that every token forced the
  pane back to the bottom. Auto-follow now disengages when the reader scrolls
  up and a `Latest` control restores it explicitly.
- Clarified everywhere that CPU-only is a backend statement, not a claim that
  arbitrary 16 GB laptops are supported. The current product requires macOS on
  Apple Silicon and has been fully exercised on one 16 GB M3 MacBook Air.
